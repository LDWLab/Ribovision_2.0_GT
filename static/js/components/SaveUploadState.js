export function addSaveStateButton (divName){
    var injectionDiv = document.querySelector(divID)
    
    
    injectionDiv.innerHTML = htmlTOinject;
}

export function addLoadStateButton (divName){
    var injectionDiv = document.querySelector(divID)
    
    
    injectionDiv.innerHTML = htmlTOinject;
}